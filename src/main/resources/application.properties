spring.application.name=eternivity-auth

# Database Configuration
spring.datasource.url=jdbc:mysql://192.168.0.106:3306/eternivity_auth?useSSL=false
spring.datasource.username=pi
spring.datasource.password=pi200997
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
## JWT Configuration
## For testing only: a randomly generated 256-bit (32-byte) secret, base64-encoded.
## In production, override via environment variable JWT_SECRET or use a secret manager.
jwt.secret=${JWT_SECRET:Zk7v9Qp4sE2rT8nV1bC0mY6hL3xW5uAqS2dFg7Hj=}
jwt.expiration=900000

# Server Configuration
server.port=8080

# Configure Jackson to write dates as ISO strings rather than timestamps/arrays
spring.jackson.serialization.write-dates-as-timestamps=false

# Optionally set date/time format (the DTOs use @JsonFormat; this is a fallback)
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss

# CORS allowed origin patterns (CSV). Only these patterns will be allowed.
# Example: http://localhost:*,https://eternivity.com
#app.cors.allowed-origins=http://localhost:*,https://eternivity.com
